# Database - Neon PostgreSQL
# Get your connection strings from your Neon dashboard: https://console.neon.tech
# Use the pooled connection string for DATABASE_URL
DATABASE_URL="postgresql://username:password@ep-xxx-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# Use the direct (non-pooled) connection string for DIRECT_URL (used for migrations)
DIRECT_URL="postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require"

# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"
# Set to your production URL when deploying (Vercel will auto-set this)
AUTH_URL="http://localhost:3000"

# Vercel automatically sets these - no need to configure manually:
# VERCEL_ENV, VERCEL_URL
